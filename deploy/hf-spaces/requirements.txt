# Hugging Face Spaces install. The Anvil package itself is installed
# editable from the cloned repo (`pip install -e .` in the Space's
# pre-build step) so it pulls in pydantic + huggingface_hub + openai +
# rapidfuzz from anvil's pyproject. Spaces-specific additions below.
gradio>=5.0,<6.0
torch>=2.4
transformers>=4.50,<5.0
peft>=0.13,<0.20
accelerate>=1.0,<2.0
