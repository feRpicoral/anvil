# Eval-time constraints for ad hoc paid-eval installs.
#
# The base project already carries the API clients and schema validators.
# Keep NumPy below 2.3 so CUDA/eval transitive deps do not drift into the
# resolver mismatch hit during the Forge RunPod run.

numpy<2.3
